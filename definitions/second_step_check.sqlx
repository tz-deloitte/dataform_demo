config {
  type: "assertion",
  name: "check_for_O"
}

SELECT 
  CASE 
    WHEN EXISTS (SELECT 1 FROM ref{{("staging_table_dataform")}} WHERE O_ORDERSTATUS = 'O') 
    THEN TRUE 
    ELSE FALSE 
  END AS has_ones